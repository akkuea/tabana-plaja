{
  "name": "tabana-plaja",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "echo 'Choose an app to run, like cd apps/landing && bun dev'",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.3",
    "husky": "^9.1.7",
    "prettier": "3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "globals": "^16.3.0"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,md}": [
      "eslint --fix",
      "prettier --write --ignore-unknown"
    ]
  }
}
